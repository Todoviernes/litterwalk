<%= form_for @can do |f| %>
  <fieldset class="form-group">
    <%= f.label :What_is_the_issue %>
    <%= f.select :issues, [['Can is Full'], ['Can is Damaged'], ['Can is Gone']], class: "form-control" %>
  </fieldset>

  <%= f.submit 'Add!',  class: 'btn btn-primary' %>
<% end %>