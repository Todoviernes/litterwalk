<%= form_with(model: can, local: true) do |form| %>
  <% if can.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(can.errors.count, "error") %> prohibited this can from being saved:</h2>

      <ul>
      <% can.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :can_type %>
    <%= form.text_field :can_type, id: :can_can_type %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :can_address %>
  </div>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude, id: :can_latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude, id: :can_longitude %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
